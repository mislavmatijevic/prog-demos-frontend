import { ComplexityDescriptionPipe } from './complexity-description.pipe';

describe('ComplexityDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplexityDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
