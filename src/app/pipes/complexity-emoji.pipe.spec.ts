import { ComplexityEmojiPipe } from './complexity-emoji.pipe';

describe('ComplexityEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplexityEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
